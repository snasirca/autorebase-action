name: Auto-Rebase

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger rebase
        uses: snasirca/autorebase-action@main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
